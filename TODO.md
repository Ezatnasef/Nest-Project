# TODO: Add HTTP Requests to test.http

- [x] Add POST /users request for user creation
- [x] Add PATCH /artists/:id request for updating an artist
- [x] Add DELETE /artists/:id request for deleting an artist
- [x] Add GET / request for the app root endpoint
- [x] Ensure all requests include necessary headers (Content-Type, Authorization)
- [x] Organize the file with clear sections for each module (Auth, Users, Artists, Songs, App)
- [x] Add placeholders or notes for tokens/keys where needed
- [x] Add missing endpoints: POST /users, GET /users/:id, PATCH /users/:id, DELETE /users/:id, GET /artists/:id, DELETE /artists/:id, GET /songs, GET /songs/:id, POST /songs (with artist role), GET /songs (pagination)
- [x] Fix the file structure to include all endpoints properly
